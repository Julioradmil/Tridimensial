/* Paleta de colores */
:root {
  --color-primario: #0077ff;
  --color-secundario: #7fd81a;
  --color-claro: #f5f5f5;
  --color-gris: #d0d0d0;
  --color-texto: #e6e6e6;
}

/* Reset básico */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Poppins', sans-serif;
}

body {
  background-color: var(--color-secundario);
  color: var(--color-texto);
  line-height: 1.6;
}

/* Encabezado */
header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: rgba(0, 0, 0, 0.8);
  padding: 1rem 2rem;
  position: fixed;
  width: 100%;
  top: 0;
  z-index: 10;
}

.logo {
  font-size: 1.5rem;
  font-weight: bold;
  color: var(--color-primario);
}

nav ul {
  display: flex;
  list-style: none;
}

nav ul li {
  margin-left: 1.5rem;
}

nav ul li a {
  color: var(--color-texto);
  text-decoration: none;
  transition: 0.3s;
}

nav ul li a:hover {
  color: var(--color-primario);
}

/* Hero */
.hero {
  background: linear-gradient(to right, rgba(0,0,0,0.7), rgba(10,25,47,0.8)),
              url('img/hero-bg.jpg') center/cover no-repeat;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  padding: 0 1rem;
}

.hero-content h1 {
  font-size: 2.5rem;
  margin-bottom: 1rem;
}

.hero-content p {
  font-size: 1.2rem;
  margin-bottom: 1.5rem;
}

.btn {
  background: var(--color-primario);
  color: white;
  padding: 0.8rem 1.5rem;
  border-radius: 25px;
  text-decoration: none;
  transition: 0.3s;
}

.btn:hover {
  background: #005fd1;
}

/* Servicios */
.servicios {
  padding: 5rem 2rem;
  text-align: center;
  background: var(--color-claro);
  color: #222;
}

.servicios h2 {
  font-size: 2rem;
  margin-bottom: 2rem;
}

.servicios-container {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
}

.card {
  background: white;
  border-radius: 10px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
  padding: 1.5rem;
  transition: transform 0.3s;
}

.card:hover {
  transform: translateY(-5px);
}

.card img {
  width: 100%;
  border-radius: 10px;
  margin-bottom: 1rem;
}

/* Galería */
.galeria {
  padding: 5rem 2rem;
  text-align: center;
}

.galeria h2 {
  margin-bottom: 2rem;
}

.galeria-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1rem;
}

.galeria-grid img {
  width: 100%;
  border-radius: 10px;
  transition: transform 0.3s;
}

.galeria-grid img:hover {
  transform: scale(1.05);
}

/* Contacto */
.contacto {
  padding: 5rem 2rem;
  text-align: center;
  background: var(--color-claro);
  color: #222;
}

form {
  display: flex;
  flex-direction: column;
  max-width: 500px;
  margin: auto;
}

input, textarea, button {
  margin-bottom: 1rem;
  padding: 0.8rem;
  border: 1px solid var(--color-gris);
  border-radius: 5px;
  font-size: 1rem;
}

button {
  background: var(--color-primario);
  color: white;
  border: none;
  cursor: pointer;
  transition: 0.3s;
}

button:hover {
  background: #005fd1;
}

/* Footer */
footer {
  text-align: center;
  padding: 2rem;
  background: #000;
  color: var(--color-texto);
}

footer .redes a {
  color: var(--color-primario);
  text-decoration: none;
  margin: 0 0.5rem;
  transition: 0.3s;
}

footer .redes a:hover {
  color: white;
}

/* Responsive */
@media (max-width: 768px) {
  nav ul {
    flex-direction: column;
    background: rgba(0,0,0,0.9);
    position: absolute;
    top: 70px;
    right: 0;
    padding: 1rem;
    display: none;
  }
}
